class ArithemticModel {
  final int first;
  final int second;

  ArithemticModel({
    required this.first,
    required this.second,
  });

  // Add
  int add() {
    return first + second + 1;
  }

  // Sub
}
